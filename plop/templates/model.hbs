

import { Schema, model } from "mongoose"
import type { {{>pascalName }} } from "../../shared/types"

const {{ camelCase name }}Schema = new Schema<{{>pascalName }}>(
    {
        
    },
    { timestamps: true }
)

export const {{>pascalName }}Model = model<{{>pascalName }}>("{{>pascalName }}", {{ camelCase name }}Schema)
